  
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> Need to be Devoured!<br>
    <input type="radio" name="devoured" value="1">Devoured
  </div>

  <button type="submit">Add Burger</button>
</form>
<h1>Eat Da Burger!</h1>

<h2>A wish list of Burgers to be Devoured! Yum!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    <p>{{id}}. {{burger_name}}</p>
    <span><button class="change-devoured" data-id"{{id}}" data-newdevoured=true>
      {{#if devoured}} Devoured! {{else}} EAT ME! {{/if}}
    </button></span>
    </p>
    {{/unless}}
  {{/each}}
</ul>

<h2>List of Devoured Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <p>{{id}}. {{burger_name}}</p>
    {{/if}}
  {{/each}}
</ul>