h1 class="jumbotron" id="title">Eat-Da-Burger <img id="burgerPic" src="/assets/images/burger.jpg"
        alt="Burger Pic" width="100px" height="100px"></h1>

<div class="container">
    <div class="burgers">

        <h3 class="burgers-title">Burgers</h3>

        <ul>

            {{#each burgers}}
            {{#unless this.devoured}}

            <li class="list-of-burgers rounded">{{this.burger_name}} <button id="devourBtn" class="devourBtn btn-primary rounded"
                    data-id="{{this.id}}">Devour</button></li>


            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="burgers">

        <h3 class="burgers-title">Devoured Burgers</h3>

        <ul>

            {{#each burgers}}
            {{#if this.devoured}}

            <li class="list-of-eaten-burgers">{{this.burger_name}}<button id="deleteBtn" class="deleteBtn btn-danger rounded"
                    data-id="{{this.id}}">Delete</button></li>


            {{/if}}
            {{/each}}

        </ul>

    </div>

   
    <div>
    <form class="createBurger" id="burgerEntry">

        <div class="form-group">
            <textarea class="rounded"  rows="2" cols="50" type="text" id="burgerName" name="name"></textarea>
            <hr>
            <button id="submit" class="btn-info rounded">Add Burger</button>
            <p><strong>Enter the name of the Burger you would like to eat...</strong></p>
        </div>
        

    </form>
    </div>

</div>

{{!--   
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="burger_name">
  </div>

  {{!-- <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="0"> Need to be Devoured!<br>
    <input type="radio" name="devoured" value="1">Devoured
  </div> --}}

  <button type="submit">Add Burger</button>
</form>
<h1>Eat Da Burger!</h1>

<h2>A wish list of Burgers to be Devoured! Yum!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    <p>{{id}}. {{burger_name}}</p>
    <span><button class="change-devoured" data-id="{{id}}">
      {{#if devoured}} Devoured! {{else}} EAT ME! {{/if}}
    </button></span>
    </p>
    {{/unless}}
  {{/each}}
</ul>

<h2>List of Devoured Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <p>{{id}}. {{burger_name}}</p>
    {{/if}}
  {{/each}}
</ul> --}}